# ============================================================
# Jetson Orin Nano Super / JetPack 6.x (aarch64)
# ============================================================
# PyTorch and torchvision are pre-installed in the l4t-pytorch
# base image with CUDA support — do NOT list them here.
# ============================================================

fastapi==0.111.0
uvicorn==0.30.1
pillow==10.4.0
numpy<2

# Pin basicsr to 1.4.2 — versions 1.4.3+ import
# torchvision.transforms.functional_tensor which was removed in torchvision 0.17.
basicsr==1.4.2
realesrgan==0.3.0

# Use headless OpenCV (no GUI dependencies) built for aarch64
opencv-python-headless
